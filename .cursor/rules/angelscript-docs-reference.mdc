---
description: 编辑 .as 按 write-angelscript；保存即生效；文档/API 以该技能与 reference 为准
globs: "**/*.as,**/*.cpp,**/*.h"
alwaysApply: true
---

# AngelScript 编写与参考（指向技能）

编写或修改 **AngelScript**、或涉及 AS 与 UE 交互的 C++ 时：

- **流程、文档路径**（`content/reference/AS_API/`、`Docs-UE-Angelscript/`）、API 查阅顺序、写法约定：技能 **write-angelscript** 为唯一权威。
- **保存即生效**：AS 保存后热重载，无需单独编译；细节见 **write-angelscript**。
