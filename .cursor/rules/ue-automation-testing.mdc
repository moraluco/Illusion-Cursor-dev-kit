# UE 自动化测试（通用入口）

当用户提出以下需求时（任一）：

- “进 UE 做自动化测试 / 自动化验证”
- “并发跑 UE 自动化”
- “soft-ue-cli / SoftUEBridge 测试”
- “UnrealEditor-Cmd 无人值守测试 / CI”

优先按技能 **ue-automation-test-harness** 执行，先做模式选择与治理，再写脚本/跑测试：

- **Editor+Bridge**：交互式 `UnrealEditor.exe` + SoftUEBridge（soft-ue-cli）
- **Editor-Cmd**：`UnrealEditor-Cmd.exe`（unattended/Commandlet；以退出码+Saved/Logs 为主）

强制约定（避免高频踩坑）：

- **不要直接多处 `Start-Process UnrealEditor.exe`**：统一用 `content/dev/scripts/Start-UnrealEditor.ps1` 管理复用/门禁/互斥。
- **并发任务必须隔离输出目录**（OutDir/SnapshotDir），避免文件锁把问题伪装成“桥干扰”。
- PowerShell 脚本中避免使用变量名 `$pid/$Pid`（会撞到只读 `$PID`）。

