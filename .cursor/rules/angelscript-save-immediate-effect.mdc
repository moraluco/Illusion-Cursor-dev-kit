---
description: AngelScript 保存即生效、无需编译。写 AS 时见 skill write-angelscript。
globs: "**/*.as,**/*.cpp,**/*.h"
alwaysApply: true
---

# AngelScript 保存即生效（指向技能）

本 Rule 仅在编辑 `.as` 或涉及 AS 时做触发与提醒：

- **保存即生效**：AS 保存后即热重载，无需单独编译；具体说明与工作流以技能 **write-angelscript** 的「书写约定」为唯一权威来源。
- UE–AS 用法与文档路径见技能 **write-angelscript** 及其 `content/reference/Docs-UE-Angelscript/`。
