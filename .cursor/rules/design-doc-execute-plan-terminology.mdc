---
description: 讨论设计默认只改 .design.md；执行计划/实现才改 .as；Task 与清空规则见 design-doc-code-parity §0.1/§4/§5
alwaysApply: true
---

# 讨论设计 vs 执行计划

- 用户**只**在问设计、改 **`*.design.md`**、补 **`## 待执行计划`**、拆文档时：**默认只编辑文档**，**不要**同时改 **`.as`** —— 否则会破坏「先迭代等效设计，再执行计划」的流程。细则 **design-doc-code-parity §0.1**。
- **只有**当用户说 **「执行计划」** / **「执行设计文档里的计划」** / **「对齐代码」** / **「实现」** / 明确改某 **`*.as`** 时，才按 Task 改源码与测试。

---

# 「执行计划」指什么（与 Cursor 计划区分）

用户只说 **「执行计划」**、**「落实末尾计划」**、**「执行设计文档中的计划」**、**「按计划改代码」** 且**未**附带具体文件路径时：

- **默认**：执行 **`*.design.md`** 末尾 **`## 待执行计划`** 内 **Task 注册表** 中状态 **非「已实现」** 的 Task（见 **design-doc-code-parity** §4.1）；**逐条**改 **代码/测试** 并 **逐条**更新注册表——**禁止**只把整节标成「已实现」而子 Task 未标。
- **不默认**：Cursor/IDE 在 **`.cursor/plans/*.plan.md`**（或会话里生成的 `*.plan.md`）中的步骤清单；对话里助手**自拟**的 Markdown 待办——除非用户 **显式** 说「按 Cursor 计划文件」或 **给出该文件路径**。

## 执行时禁止「只改 design」

- **执行计划** = 按 **Task id** 修改 **等效 `.as` / 测试**，每完成一条即在 **Task 注册表** 标 **已实现**；**全部** Task 已实现后 **整节删除** `## 待执行计划`（不堆积「已实现」块），真值写回正文（§5.3）。
- **禁止**：只改写 `.design.md`、只改整节状态而不改代码；禁止长期保留「本节状态：已实现」但注册表仍混有未完成任务。
- **删 debug 变量 / 成员 / 行为**：须 **先动源码** 再对齐 design（**design-doc-code-parity §2.3a**）；禁止从规格正文单独删变量名冒充已删代码。

若 **design 末尾计划** 与 **Cursor plan** 同时存在且用户未说明：先 **简短确认** 一句，或 **优先** 当前焦点/最近编辑的 **`*.design.md`** 末尾 **待执行计划**。
