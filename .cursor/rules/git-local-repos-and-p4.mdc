---
description: 本地 Git 双仓库路径、中文提交、原子化、与 P4 边界
alwaysApply: true
---

# 本地 Git 与 Perforce（指向技能）

本 Rule 只做触发与边界提醒；**完整路径、联接、提交模板、嵌套 Git、TDD 粒度**见技能 **git-local-p4-workflow** 与 `content/dev/git-automation.md`。

- **双仓**：项目（ManteumTower）与 Kit 各为独立 Git；改哪边就在哪边提交，**勿跨仓混在一个 commit**。
- **本地优先**：常用 `D:\GitRepo\...` 联接至真实路径；自动化验收以**本地可重复**为主。
- **Git 根**：项目仓库根为 `...\Engine\ManteumTower`（`D:\Workspace\MT` 不是 Git 根）。`git status/log` 前先 `cd` 到正确根。
- **提交**：说明**一律中文**；编码 **UTF-8**（禁用 cmd `echo` 写中文 message）；**原子化**（一个可验证点一次提交），详见 `content/knowledge/14-git-atomic-commits-tdd.md`。
- **P4**：Agent **不执行** P4；文件只读时由人类 checkout 后再改。**Git 相关以技能与 dev 文档为准。**
