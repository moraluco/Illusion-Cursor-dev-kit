---
description: 研发知识库 Kit — 识别项目与 Kit，应用 METHODOLOGY 与 content/
alwaysApply: true
---

# 研发知识库 Kit（桥接规则）

本 Rule 使 AI 区分 **Kit 根**（文档/知识库/`.cursor`）与 **项目根**（代码与资源）。**方法论全文、content 边界、初始化步骤**以 Kit 根目录 `METHODOLOGY.md` 为准。

## 必须遵循的入口

- **「总结经验」且要写回**：技能 **summarize-to-knowledge**（回顾 → 沉淀 → 索引）；AS 专项可配合 **summarize-as-experience**。若强调**复盘自动化闭环、门禁、原子提交**，用 **retro-automation-workflow** 与 `content/knowledge/15-retro-automation-workflow.md`。
- **读/写**：仅在 Kit 下 `content/`（`plan`、`design`、`dev`、`knowledge`、`reference`），不在项目根建文档目录。
- **架构总览**：优先 `content/knowledge/01-project-architecture-overview.md`（若存在）。
- **自动化优先**：可脚本/测试/流水线重复的步骤应自动化；无人值守见 `content/dev/unattended-ue-automation.md`；AS 测试见 **angelscript-tdd-agent-iteration**；Git 长程上下文见 **git-local-p4-workflow**。
- **蓝图在线权威**：**soft-ue-cli** + SoftUEBridge → 技能 **soft-ue-cli-ue-bridge**、`content/dev/soft-ue-cli.md`；决策树见 `content/knowledge/07-blueprint-query-workflow.md`。
- **离线快照检索**：`BlueprintSnapshot/`、`.soft-ue-index/` 等（可能滞后）— 与 rule **blueprint-snapshot-search** 配合。

## 缺少 content 时

按 `METHODOLOGY.md` 的初始化步骤提示用户补齐；本 Rule 不假定具体文件名（由 content/README 说明）。
