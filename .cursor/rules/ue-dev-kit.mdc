---
description: UE 研发·知识库 Kit — 识别项目与 Kit，应用研发方法论
alwaysApply: true
---

# UE 研发·知识库 Kit（桥接规则）

本规则使 AI 在多根工作区中识别「项目」与「Kit」并应用研发方法论（设计-测试-验证-迭代、迭代计划、知识库）。Kit 只服务一个项目。

## 项目与 Kit

- 工作区 = Kit 根 + 项目根。
- **Kit 根** = 含本规则文件（ue-dev-kit.mdc）的根；文档与知识库均在 Kit 根下的 **content/**。
- **项目根** = 工作区中另一枚根（放代码与资源）；项目路径下不包含文档。
- 说「项目」即指该根，唯一，无需额外识别。

## 方法论定义

- 完整方法论（文档结构、编写约定、与 AI 协作约定、初始化步骤）见**本 Kit 根目录**下的 `METHODOLOGY.md`。
- 对文档与知识库的读/写均在 **Kit 根下的 content/**：`content/plan.md`、`content/design/`、`content/dev/`、`content/knowledge/`、`content/reference/`。不写项目路径下的 docs。
- 回答与代码相关问题时，引用**项目根**下的代码与资源。

## 缺少 content 时

若 Kit 内不存在 `content/` 或尚未初始化：

- 提示用户可要求 Agent 按 METHODOLOGY 中的「初始化步骤」完成初始化（为项目添加 rule、在 Kit 内生成 content/）。
