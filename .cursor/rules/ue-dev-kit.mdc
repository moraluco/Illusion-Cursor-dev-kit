---
description: 研发知识库 Kit — 识别项目与 Kit，应用研发方法论
alwaysApply: true
---

# 研发知识库 Kit（桥接规则）

本规则使 AI 在多根工作区中识别「项目」与「Kit」并应用研发方法论（设计-测试-验证-迭代、迭代计划、知识库）。Kit 只服务一个项目。**本规则与 METHODOLOGY 不依赖 content/ 或项目内任何具体路径、文件名或技术栈**；content 内结构由项目自定，并在 content 的 README 中说明。

## 项目与 Kit

- 工作区 = Kit 根 + 项目根。
- **Kit 根** = 含本规则文件（ue-dev-kit.mdc）的根；文档与知识库均在 Kit 根下的 **content/**。
- **项目根** = 工作区中另一枚根（放代码与资源）；项目路径下不包含文档。
- 说「项目」即指该根，唯一，无需额外识别。

## 方法论定义

- 完整方法论（文档结构、knowledge 与 dev 边界、编写约定、与 AI 协作约定、初始化步骤）见**本 Kit 根目录**下的 `METHODOLOGY.md`。
- **「总结经验」是写回动作**：用户说总结经验时，必须执行写回闭环（回顾 → 沉淀 → 索引更新），而非仅口头总结。完整流程、命名约定与 knowledge/dev 边界以技能 **summarize-to-knowledge** 为唯一权威来源；若为 **AS 专项经验**，可选用技能 **summarize-as-experience** 写回并增强 write-angelscript。
- 对文档与知识库的读/写均在 **Kit 根下的 content/**：`content/plan.md`、`content/design/`、`content/dev/`、`content/knowledge/`、`content/reference/`。不写项目路径下的文档目录。
- 回答与代码相关问题时，引用**项目根**下的代码与资源。具体技术栈、脚本与 API 的参考位置由 **content 内 knowledge/ 或 reference/ 的 README** 说明，本规则不假定具体路径或文件名。
- 回答与**项目结构、模块划分、实现位置、核心类/蓝图分布**相关的问题时，**先查阅** `content/knowledge/01-project-architecture-overview.md`（若存在），据此建立整体架构后再结合具体代码/蓝图作答。若该文件不存在，则按下一句约定处理。
- 理解 **UE 蓝图与项目结构**时，可参考 `content/knowledge/07-blueprint-snapshot-for-agent.md`；若项目根下存在 **BlueprintSnapshot**（含 `_index.json`、OVERVIEW.md、ARCHITECTURE_*.md），优先据此建立蓝图分布与核心架构印象。单蓝图 JSON 为 **schema 1.5**，含 **`_agentSummary`**（先读以判断是否相关）、**`structuredGraphs`**（优先）或 **`graphNodes`**（节点为 JSON 字符串）、**`dependencyClosure`**；详见 `content/knowledge/07-blueprint-snapshot-for-agent.md`。
- **本项目约定**：项目根下若存在 `ManteumTower/BlueprintSnapshot/`，其中的各蓝图快照 JSON（由蓝图导出插件生成）是**蓝图逻辑与调用关系的权威来源**。当需要「读蓝图实现」或说明蓝图行为时，应优先直接解析这些 JSON，而不是假定可以在编辑器中打开蓝图图表。

## 缺少 content 时

若 Kit 内不存在 `content/` 或尚未初始化：

- 提示用户可要求 Agent 按 METHODOLOGY 中的「初始化步骤」完成初始化（为项目添加 rule、在 Kit 内生成 content/）。
