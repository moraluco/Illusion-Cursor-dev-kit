---
description: 蓝图快照 JSON 仅作只读参考，修改不会影响蓝图行为；Agent 须在编辑器中改蓝图或改代码
alwaysApply: true
---

# 蓝图快照（BlueprintSnapshot）正确认知

## 快照 ≠ 蓝图

- **BlueprintSnapshot 目录下的 \*.json**（如 `BP_Enemy_Shadow.json`、`BP_Weapon_RevolverPistol.json`）是**蓝图的只读导出快照**，用于给 Agent 理解蓝图结构、节点、变量、碰撞等逻辑。
- **修改这些 JSON 文件不会改变任何蓝图行为**。引擎加载与运行时使用的是 Content 中的 **.uasset** 资产，由 Unreal 编辑器编辑并保存。
- 快照通常由 BlueprintSerializer 等插件从编辑器导出，用于版本对比、文档或给 AI 阅读；**没有“从 JSON 写回蓝图”的流程**时，改 JSON 仅改动了快照副本，不会生效。

## Agent 必须遵守

1. **只读使用**：用快照 JSON 来**阅读、分析、比对**蓝图逻辑（例如对照手枪蓝图与左轮实现、查碰撞 ResponseArray、查事件节点）。
2. **不通过改 JSON 修逻辑**：若要改变蓝图属性（如碰撞通道、组件默认值、事件连线），**必须在 UE 编辑器中打开对应蓝图进行修改**，或修改 C++/AngelScript 等实际代码。
3. **指导用户时**：若结论是“需改某蓝图”，在回复中明确写出**在编辑器中操作的步骤**（例如：打开 BP_XXX → 选中某组件 → Details → Collision → 将 Damage 设为 Block），而不要通过编辑 `BlueprintSnapshot/BP_XXX.json` 来“修复”。

## 正确改法示例

- **错误**：在 `BP_Enemy_Shadow.json` 里给 `ResponseArray` 添加 `Damage=Block`，期望敌人被射线命中。
- **正确**：在回复中说明“在编辑器中打开 BP_Enemy_Shadow → 选中 CharacterMesh0 → Details → Collision → Collision Responses → Damage 设为 Block”，并可将该步骤写入 EditorSteps 等文档；如需版本可见的变更，再考虑在项目内通过编辑器保存 .uasset。
