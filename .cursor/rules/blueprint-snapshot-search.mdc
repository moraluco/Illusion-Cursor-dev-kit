---
description: 大范围搜索时刷新蓝图快照并优先搜快照；与在线读图分工见 knowledge
alwaysApply: true
---

# 蓝图快照优先（Search/Grep）

**大范围**搜蓝图（全局找函数/变量/注释/节点、「谁在用 X」）时：

1. 运行 `content/dev/scripts/Ensure-BlueprintSnapshot.ps1` 保证快照存在且尽量新鲜。
2. 先搜 `BlueprintSnapshot/blueprints_full.txt` 与 `.ndjson`。
3. 仍不足或要**精确图结构/最新事实**时，再用 soft-ue-cli 在线查询或搜源码。

在线 vs 离线决策树、取证落地文件：`content/knowledge/07-blueprint-query-workflow.md`。桥不可用时明确说明仅离线+源码。
