---
description: 等效 `*.design.md`、从代码撰写规格、末尾计划小节、严格对齐实现；完整流程见技能 design-doc-code-parity
globs: "**/*.design.md"
alwaysApply: false
---

# 代码等效设计文档（指向技能）

涉及 **`*.design.md`**（与源码同目录的逻辑规格）、**严格等效**改代码、**撰写/更新**规格、**拆分** `*-deferred.design.md`、或按 **`## 待执行计划`**（含 **Task 注册表**）分阶段改 AS/测试时：

- **用户只讨论/修改设计**（未说执行计划或实现）：**默认只动** `.design.md`，**不**自动改 `.as` —— 技能 **design-doc-code-parity §0.1**。
- **用户说「执行计划」/「执行设计文档中的计划」等且无路径时**：按 **Task 注册表** 逐条执行；**全已实现** 后 **整节清空**（不堆积）——见 Rule **design-doc-execute-plan-terminology** 与技能 **design-doc-code-parity** §0、§4.1、§4.2、§5.3。
- **删变量 / 调试 / 行为变更**：**先改 `*.as`**，再改 design 规格与 **待执行计划** 状态；**禁止**只从 `.design.md` 抠掉变量或「文档删了当代码删了」——见技能 **§2.3a**。
- **完整步骤、模板、引用门禁、执行后移除已完成计划与 TDD 提交节奏**：技能 **design-doc-code-parity**（Kit：`.cursor/skills/design-doc-code-parity/SKILL.md`）。
- **编写/编辑 `.as`**：技能 **write-angelscript**；跑测：**angelscript-tdd-agent-iteration**；**Git 根与中文原子提交**：**git-local-p4-workflow**、**git-commit-zh**（项目脚本仓在 `Engine\ManteumTower`，勿与 Kit 混提交）。
- **删成员/API 前引用检索**：离线索引 **blueprint-snapshot-search**；「当前编辑器内」蓝图事实以 **soft-ue-cli-ue-bridge** 在 `check-setup` 成功后为准。

概念索引（可选阅读）：`content/knowledge/16-design-doc-equivalent-workflow.md`。
