# UNiagaraDataInterfaceVolumeTexture

**Visibility:** public

## Inheritance

UNiagaraDataInterface → UNiagaraDataInterfaceVolumeTexture

## Description

Data Interface allowing sampling of a texture

## TEXTURE

### TextureUserParameter

```
FNiagaraUserParameterBinding TextureUserParameter
```

When valid the user parameter is used as the texture rather than the one on the data interface

### Texture

```
UVolumeTexture Texture STATIC FUNCTIONS StaticClass static UClass UNiagaraDataInterfaceVolumeTexture::StaticClass()
```
