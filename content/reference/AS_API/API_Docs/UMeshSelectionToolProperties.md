# UMeshSelectionToolProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → UMeshSelectionToolProperties → SELECTION → AngleTolerance

## Description

float32 AngleTolerance

Angle in Degrees used for Angle-based Selection Modes

bHitBackFaces bool bHitBackFaces

Allow the brush to hit back-facing parts of the surface

bShowPoints bool bShowPoints

Toggle drawing of highlight points on/off

FaceColorMode EMeshFacesColorMode FaceColorMode

Color each triangle based on the selected mesh attribute

SelectionMode EMeshSelectionToolPrimaryMode SelectionMode

The Selection Mode defines the behavior of the selection brush

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UMeshSelectionToolProperties::StaticClass()
```
