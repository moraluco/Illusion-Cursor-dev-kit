# UBaseMediaSource

**Visibility:** public

## Inheritance

UMediaSource → UBaseMediaSource → UFileMediaSource → UImgMediaSource → UStreamMediaSource → UTimeSynchronizableMediaSource

## Description

Base class for concrete media sources.

## PLATFORMS

### PlatformPlayerNames

```
TMap<FString,FName> PlatformPlayerNames
```

Override native media player plug-ins per platform (Empty = find one automatically).

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UBaseMediaSource::StaticClass()
```
