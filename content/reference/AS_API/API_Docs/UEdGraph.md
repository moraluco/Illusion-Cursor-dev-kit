# UEdGraph

**Visibility:** public

## Inheritance

UObject → UEdGraph → UAIGraph → UAnimationGraph → UAnimationStateMachineGraph → UBehaviorTreeDecoratorGraph

## Description

and 8 other children STATIC FUNCTIONS StaticClass static UClass UEdGraph::StaticClass()
