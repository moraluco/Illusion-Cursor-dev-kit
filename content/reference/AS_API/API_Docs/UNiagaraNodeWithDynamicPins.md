# UNiagaraNodeWithDynamicPins

**Visibility:** public

## Inheritance

UNiagaraNode → UNiagaraNodeWithDynamicPins → UNiagaraNodeConvert → UNiagaraNodeEmitter → UNiagaraNodeFunctionCall → UNiagaraNodeIf

## Description

and 4 other children

A base node for niagara nodes with pins which can be dynamically added and removed by the user.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UNiagaraNodeWithDynamicPins::StaticClass()
```
