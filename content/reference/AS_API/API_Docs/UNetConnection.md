# UNetConnection

**Visibility:** public

## Inheritance

UPlayer → UNetConnection → UChildConnection → UDemoNetConnection → UIpConnection → UReplayNetConnection → USimulatedClientNetConnection

## Description

STATIC FUNCTIONS StaticClass static UClass UNetConnection::StaticClass()
