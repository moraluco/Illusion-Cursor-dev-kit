# UMaterialEditorSettings

**Visibility:** public

## Inheritance

UObject → UMaterialEditorSettings

## Description

USER INTERFACE DOMAIN DefaultPreviewHeight int DefaultPreviewHeight

The height (in pixels) of the preview viewport when a material editor is first opened

PreviewBackground FPreviewBackgroundSettings PreviewBackground

Configures the background shown behind the UI material preview

DefaultPreviewWidth int DefaultPreviewWidth

The width (in pixels) of the preview viewport when a material editor is first opened

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UMaterialEditorSettings::StaticClass()
```
