# ImageWrite

## Inheritance

ImageWrite → TEXTURE → ExportToDisk

## Description

static void ImageWrite::ExportToDisk( UTexture 	Texture, FString 	Filename, FImageWriteOptions 	Options 	)

Export the specified texture to disk

Parameters Texture UTexture

The texture or render target to export

Filename FString

The filename on disk to save as

Options FImageWriteOptions

Parameters defining the various export options
