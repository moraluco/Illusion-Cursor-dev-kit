# UCameraShakePattern

**Visibility:** public

## Inheritance

UObject → UCameraShakePattern → UCompositeCameraShakePattern → UMatineeCameraShakePattern → USequenceCameraShakePattern → USimpleCameraShakePattern

## Description

A shake "pattern" defines how a camera should be effectively shaken. Examples of shake patterns are sinewave oscillation, perlin noise, or FBX animation.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UCameraShakePattern::StaticClass()
```
