# ACameraBlockingVolume

**Visibility:** public

## Inheritance

AVolume → ACameraBlockingVolume

## Description

A volume which blocks the Camera channel by default.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static ACameraBlockingVolume ACameraBlockingVolume::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass ACameraBlockingVolume::StaticClass

```
static UClass ACameraBlockingVolume::StaticClass()
```
