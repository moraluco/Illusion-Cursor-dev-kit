# UNiagaraDataInterfaceGrid3D

**Visibility:** public

## Inheritance

UNiagaraDataInterfaceRWBase → UNiagaraDataInterfaceGrid3D → UNiagaraDataInterfaceGrid3DCollection → UNiagaraDataInterfaceNeighborGrid3D → UNiagaraDataInterfaceRasterizationGrid3D → GRID → CellSize

## Description

float32 CellSize

World space size of a cell

NumCellsMaxAxis int NumCellsMaxAxis

Number of cells on the longest axis

SetResolutionMethod ESetResolutionMethod SetResolutionMethod

Method for setting the grid resolution

WorldBBoxSize FVector WorldBBoxSize

World size of the grid

NumCells FIntVector NumCells

Number of cells

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UNiagaraDataInterfaceGrid3D::StaticClass()
```
