# ATriggerSphere

**Visibility:** public

## Inheritance

ATriggerBase → ATriggerSphere

## Description

A sphere shaped trigger, used to generate overlap events in the level

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static ATriggerSphere ATriggerSphere::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass ATriggerSphere::StaticClass

```
static UClass ATriggerSphere::StaticClass()
```
