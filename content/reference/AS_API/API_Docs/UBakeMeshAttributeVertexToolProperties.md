# UBakeMeshAttributeVertexToolProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → UBakeMeshAttributeVertexToolProperties → BAKEOUTPUT → OutputTypeB

## Description

int OutputTypeB

The bake output type to generate in the Blue channel

OutputType int OutputType

The bake output type to generate

OutputTypeR int OutputTypeR

The bake output type to generate in the Red channel

OutputTypeG int OutputTypeG

The bake output type to generate in the Green channel

OutputMode EBakeVertexOutput OutputMode

The bake output mode

OutputTypeA int OutputTypeA

The bake output type to generate in the Alpha channel

PreviewMode EBakeVertexChannel PreviewMode

The vertex color channel to preview

bSplitAtNormalSeams bool bSplitAtNormalSeams

If true, compute a separate vertex color for each unique normal on a vertex

bSplitAtUVSeams bool bSplitAtUVSeams

If true, Compute a separate vertex color for each unique UV on a vertex.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UBakeMeshAttributeVertexToolProperties::StaticClass()
```
