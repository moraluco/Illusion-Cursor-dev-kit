# APaperTerrainActor

**Visibility:** public

## Inheritance

AActor → APaperTerrainActor

## Description

An instance of a piece of 2D terrain in the level

## SPRITE

### RenderComponent

```
UPaperTerrainComponent RenderComponent
```
### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static APaperTerrainActor APaperTerrainActor::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass APaperTerrainActor::StaticClass

```
static UClass APaperTerrainActor::StaticClass()
```
