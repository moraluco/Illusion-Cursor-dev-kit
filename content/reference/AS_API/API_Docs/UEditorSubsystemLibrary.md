# UEditorSubsystemLibrary

**Visibility:** public

## Inheritance

UObject → UEditorSubsystemLibrary

## Description

These functions are blueprint internal by default, but we need them exposed in Angelscript

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UEditorSubsystemLibrary::StaticClass()
```
