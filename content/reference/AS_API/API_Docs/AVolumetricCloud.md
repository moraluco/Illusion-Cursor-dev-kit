# AVolumetricCloud

**Visibility:** public

## Inheritance

AInfo → AVolumetricCloud

## Description

A placeable actor that represents a participating media material around a planet, e.g. clouds.  @see TODO address to the documentation.

## ATMOSPHERE

### VolumetricCloudComponent

```
UVolumetricCloudComponent VolumetricCloudComponent
```
### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static AVolumetricCloud AVolumetricCloud::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass AVolumetricCloud::StaticClass

```
static UClass AVolumetricCloud::StaticClass()
```
