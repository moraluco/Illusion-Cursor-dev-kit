# UAnimPreviewInstance

**Visibility:** public

## Inheritance

UAnimSingleNodeInstance → UAnimPreviewInstance

## Description

This Instance only contains one AnimationAsset, and produce poses Used by Preview in AnimGraph, Playing single animation in Kismet2 and etc

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UAnimPreviewInstance::StaticClass()
```
