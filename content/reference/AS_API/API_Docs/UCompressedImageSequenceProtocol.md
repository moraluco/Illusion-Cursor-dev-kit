# UCompressedImageSequenceProtocol

**Visibility:** public

## Inheritance

UImageSequenceProtocol → UCompressedImageSequenceProtocol → UImageSequenceProtocol_JPG → UImageSequenceProtocol_PNG → IMAGESETTINGS → CompressionQuality

## Description

int CompressionQuality

Level of compression to apply to the image, between 1 (worst quality, best compression) and 100 (best quality, worst compression)

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UCompressedImageSequenceProtocol::StaticClass()
```
