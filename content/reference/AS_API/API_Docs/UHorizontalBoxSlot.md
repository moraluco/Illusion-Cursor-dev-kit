# UHorizontalBoxSlot

**Visibility:** public

## Inheritance

UPanelSlot → UHorizontalBoxSlot

## Description

LAYOUT|HORIZONTAL BOX SLOT Size FSlateChildSize Size

How much space this slot should occupy in the direction of the panel.

HorizontalAlignment EHorizontalAlignment HorizontalAlignment VerticalAlignment EVerticalAlignment VerticalAlignment Padding FMargin Padding

The amount of padding between the slots parent and the content.

LAYOUT|HORIZONTAL BOX SLOT SetPadding void SetPadding( FMargin 	InPadding 	) SetSize void SetSize( FSlateChildSize 	InSize 	) SetVerticalAlignment void SetVerticalAlignment( EVerticalAlignment 	InVerticalAlignment 	) SetHorizontalAlignment void SetHorizontalAlignment( EHorizontalAlignment 	InHorizontalAlignment 	)

## FUNCTIONS

### GetPadding

```
const FMargin& GetPadding()const
```

GetSize

### const FSlateChildSize& GetSize

```
const FSlateChildSize& GetSize()const GetHorizontalAlignment
```

const EHorizontalAlignment& GetHorizontalAlignment() const

### GetVerticalAlignment

```
const EVerticalAlignment& GetVerticalAlignment()const
```

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UHorizontalBoxSlot::StaticClass()
```
