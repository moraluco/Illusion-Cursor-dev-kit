# APartyBeaconHost

**Visibility:** public

## Inheritance

AOnlineBeaconHostObject → APartyBeaconHost

## Description

A beacon host used for taking reservations for an existing game session

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static APartyBeaconHost APartyBeaconHost::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass APartyBeaconHost::StaticClass

```
static UClass APartyBeaconHost::StaticClass()
```
