# UTextureLightProfile

**Visibility:** public

## Inheritance

UTexture2D → UTextureLightProfile → TEXTURELIGHTPROFILE → Brightness

## Description

float32 Brightness

Light brightness in Candelas, imported from IES profile, <= 0 if the profile is used for masking only. Use with InverseSquareFalloff.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UTextureLightProfile::StaticClass()
```
