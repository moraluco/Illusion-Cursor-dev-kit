# AExponentialHeightFog

**Visibility:** public

## Inheritance

AInfo → AExponentialHeightFog

## Description

Implements an Actor for exponential height fog.

## FUNCTIONS

### SetComponent

```
void SetComponent(UExponentialHeightFogComponent Value)
```

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static AExponentialHeightFog AExponentialHeightFog::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass AExponentialHeightFog::StaticClass

```
static UClass AExponentialHeightFog::StaticClass()
```
