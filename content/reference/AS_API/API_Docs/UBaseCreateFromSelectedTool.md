# UBaseCreateFromSelectedTool

**Visibility:** public

## Inheritance

UMultiSelectionMeshEditingTool → UBaseCreateFromSelectedTool → UBaseVoxelTool → UCSGMeshesTool → UCutMeshWithMeshTool → USelfUnionMeshesTool

## Description

UBaseCreateFromSelectedTool is a base Tool (must be subclassed) that provides support for common functionality in tools that create a new mesh from a selection of one or more existing meshes

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UBaseCreateFromSelectedTool::StaticClass()
```
