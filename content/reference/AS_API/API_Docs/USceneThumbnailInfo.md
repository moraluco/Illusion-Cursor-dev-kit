# USceneThumbnailInfo

**Visibility:** public

## Inheritance

UThumbnailInfo → USceneThumbnailInfo → USceneThumbnailInfoWithPrimitive → UWorldThumbnailInfo → THUMBNAIL → OrbitYaw

## Description

float32 OrbitYaw

The yaw of the orbit camera around the asset

OrbitZoom float32 OrbitZoom

The offset from the bounds sphere distance from the asset

OrbitPitch float32 OrbitPitch

The pitch of the orbit camera around the asset

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass USceneThumbnailInfo::StaticClass()
```
