# UTextureRenderTarget

**Visibility:** public

## Inheritance

UTexture → UTextureRenderTarget → UTextureRenderTarget2D → UTextureRenderTarget2DArray → UTextureRenderTargetCube → UTextureRenderTargetVolume → TEXTURERENDERTARGET → TargetGamma

## Description

float32 TargetGamma

Will override FTextureRenderTarget2DResource::GetDisplayGamma if > 0.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UTextureRenderTarget::StaticClass()
```
