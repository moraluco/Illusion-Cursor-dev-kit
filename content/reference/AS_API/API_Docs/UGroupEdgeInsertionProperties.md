# UGroupEdgeInsertionProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → UGroupEdgeInsertionProperties → INSERTEDGE → VertexTolerance

## Description

float VertexTolerance

How close a new loop edge needs to pass next to an existing vertex to use that vertex rather than creating a new one (used for plane cut).

InsertionMode EGroupEdgeInsertionMode InsertionMode

Determines how group edges are added to the geometry

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UGroupEdgeInsertionProperties::StaticClass()
```
