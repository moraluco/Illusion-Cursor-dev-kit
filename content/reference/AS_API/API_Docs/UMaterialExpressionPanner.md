# UMaterialExpressionPanner

**Visibility:** public

## Inheritance

UMaterialExpression → UMaterialExpressionPanner → MATERIALEXPRESSIONPANNER → SpeedY

## Description

float32 SpeedY ConstCoordinate uint ConstCoordinate

only used if Coordinate is not hooked up

bFractionalPart bool bFractionalPart

Output only the fractional part of the pan calculation for greater precision.  Output is greater than or equal to 0 and less than 1.

SpeedX float32 SpeedX STATIC FUNCTIONS StaticClass static UClass UMaterialExpressionPanner::StaticClass()
