# UIKRig_SetTransformEffector

**Visibility:** public

## Inheritance

UObject → UIKRig_SetTransformEffector

## Description

SET TRANSFORM EFFECTOR bEnableRotation bool bEnableRotation

If true, Goal will drive the rotation of the target bone. Default is true.

Alpha float32 Alpha

Blend the effector on/off. Range is 0-1. Default is 1.0.

bEnablePosition bool bEnablePosition

If true, Goal will drive the translation of the target bone. Default is true.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UIKRig_SetTransformEffector::StaticClass()
```
