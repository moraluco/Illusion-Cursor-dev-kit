# UMaterialExpressionTextureCoordinate

**Visibility:** public

## Inheritance

UMaterialExpression → UMaterialExpressionTextureCoordinate → MATERIALEXPRESSIONTEXTURECOORDINATE → UTiling

## Description

float32 UTiling

Controls how much the texture tiles horizontally, by scaling the U component of the vertex UVs by the specified amount.

VTiling float32 VTiling

Controls how much the texture tiles vertically, by scaling the V component of the vertex UVs by the specified amount.

CoordinateIndex int CoordinateIndex

Texture coordinate index

## FUNCTIONS

### SetUnMirrorV

```
void SetUnMirrorV(bool Value)
```

SetUnMirrorU

### void SetUnMirrorU

```
void SetUnMirrorU(bool Value)
```

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UMaterialExpressionTextureCoordinate::StaticClass()
```
