# ULineSplineGenerator

**Visibility:** public

## Inheritance

USplineGeneratorBase → ULineSplineGenerator → SHAPEPARAMETERS → Length

## Description

float32 Length bUpToNextPoint bool bUpToNextPoint

If enabled, will add points up until the next existing point

NumberOfPoints int NumberOfPoints STATIC FUNCTIONS StaticClass static UClass ULineSplineGenerator::StaticClass()
