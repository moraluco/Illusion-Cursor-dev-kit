# UBaseCreateFromSelectedHandleSourceProperties

**Visibility:** public

## Inheritance

UOnAcceptHandleSourcesProperties → UBaseCreateFromSelectedHandleSourceProperties → OUTPUTOBJECT → OutputNewName

## Description

FString OutputNewName

Base name of the newly generated object to which the output is written to.

OutputWriteTo EBaseCreateFromSelectedTargetType OutputWriteTo

Defines the object the tool output is written to.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UBaseCreateFromSelectedHandleSourceProperties::StaticClass()
```
