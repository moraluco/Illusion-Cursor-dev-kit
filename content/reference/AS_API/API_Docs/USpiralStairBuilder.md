# USpiralStairBuilder

**Visibility:** public

## Inheritance

UEditorBrushBuilder → USpiralStairBuilder → BRUSHSETTINGS → StepWidth

## Description

int StepWidth

The width of each step

StepHeight int StepHeight

The height of each step

StepThickness int StepThickness

The thickness of each step

NumStepsPer360 int NumStepsPer360

The number of steps in one whole spiral rotation

NumSteps int NumSteps

The total number of steps

InnerRadius int InnerRadius

The radius of the inner curve of the stair

## FUNCTIONS

### SetSlopedFloor

```
void SetSlopedFloor(bool Value)
```

SetCounterClockwise

### void SetCounterClockwise

```
void SetCounterClockwise(bool Value)
```

SetSlopedCeiling

### void SetSlopedCeiling

```
void SetSlopedCeiling(bool Value)
```

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass USpiralStairBuilder::StaticClass()
```
