# UAnimBlueprintExtension

**Visibility:** public

## Inheritance

UBlueprintExtension → UAnimBlueprintExtension → UAnimBlueprintExtension_Attributes → UAnimBlueprintExtension_Base → UAnimBlueprintExtension_BlendSpaceGraph → UAnimBlueprintExtension_CachedPose

## Description

and 6 other children

Extension that allows per-system data to be held on the anim blueprint, and per-system logic to be executed during compilation

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UAnimBlueprintExtension::StaticClass()
```
