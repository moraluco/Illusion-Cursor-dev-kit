# APivotTransformGizmo

**Visibility:** public

## Inheritance

ABaseTransformGizmo → APivotTransformGizmo

## Description

A transform gizmo on the pivot that allows you to interact with selected objects by moving, scaling and rotating.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static APivotTransformGizmo APivotTransformGizmo::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass APivotTransformGizmo::StaticClass

```
static UClass APivotTransformGizmo::StaticClass()
```
