# UAnimGraphNode_AnimDynamics

**Visibility:** public

## Inheritance

UAnimGraphNode_SkeletalControlBase → UAnimGraphNode_AnimDynamics → PREVIEW

## Description

bPreviewLive bool bPreviewLive

Preview the live physics object on the mesh

bShowLinearLimits bool bShowLinearLimits

Show linear (prismatic) limits in the viewport

bShowAngularLimits bool bShowAngularLimits

Show angular limit ranges in the viewport

bShowPlanarLimit bool bShowPlanarLimit

Show planar limit info (actual plane, plane normal) in the viewport

bShowSphericalLimit bool bShowSphericalLimit

Show spherical limits in the viewport (preview live only)

bShowCollisionSpheres bool bShowCollisionSpheres

If planar limits are enabled and the collision mode isn't CoM, draw sphere collision sizes

## SETTINGS

### Node

```
FAnimNode_AnimDynamics Node STATIC FUNCTIONS StaticClass static UClass UAnimGraphNode_AnimDynamics::StaticClass()
```
