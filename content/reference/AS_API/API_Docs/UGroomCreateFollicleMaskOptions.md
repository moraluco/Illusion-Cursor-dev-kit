# UGroomCreateFollicleMaskOptions

**Visibility:** public

## Inheritance

UObject → UGroomCreateFollicleMaskOptions → OPTIONS → RootRadius

## Description

int RootRadius

Size of the root in the follicle mask (in pixels)

Grooms TArray<FFollicleMaskOptions> Grooms

Grooms which will be use to create the follicle texture

Resolution int Resolution

Follicle mask texture resolution. The resolution will be rounded to the closest power of two.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UGroomCreateFollicleMaskOptions::StaticClass()
```
