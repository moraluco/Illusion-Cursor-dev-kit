# UVolumeToMeshToolProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → UVolumeToMeshToolProperties → OPTIONS

## Description

bAutoRepair bool bAutoRepair bOptimizeMesh bool bOptimizeMesh bShowWireframe bool bShowWireframe bWeldEdges bool bWeldEdges STATIC FUNCTIONS StaticClass static UClass UVolumeToMeshToolProperties::StaticClass()
