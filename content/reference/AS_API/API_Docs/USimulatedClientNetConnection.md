# USimulatedClientNetConnection

**Visibility:** public

## Inheritance

UNetConnection → USimulatedClientNetConnection

## Description

A fake connection that will absorb traffic and auto ack every packet. Useful for testing scaling. Use net.SimulateConnections command to add at runtime.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass USimulatedClientNetConnection::StaticClass()
```
