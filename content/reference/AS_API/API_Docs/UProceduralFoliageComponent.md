# UProceduralFoliageComponent

**Visibility:** public

## Inheritance

UActorComponent → UProceduralFoliageComponent → PROCEDURALFOLIAGE

## Description

bAllowStaticMesh bool bAllowStaticMesh

Whether to place foliage on StaticMesh

TileOverlap float32 TileOverlap

The amount of overlap between simulation tiles (in cm).

bAllowLandscape bool bAllowLandscape

Whether to place foliage on landscape

bAllowBSP bool bAllowBSP

Whether to place foliage on BSP

FoliageSpawner UProceduralFoliageSpawner FoliageSpawner

The procedural foliage spawner used to generate foliage instances within this volume.

bAllowTranslucent bool bAllowTranslucent

Whether to place foliage on translucent geometry

bAllowFoliage bool bAllowFoliage

Whether to place foliage on other blocking foliage geometry

bShowDebugTiles bool bShowDebugTiles

Whether to visualize the tiles used for the foliage spawner simulation

DataLayer FActorDataLayer DataLayer

Data Layer to assign to foliage actors generated by foliage spawner simulation

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Get static UProceduralFoliageComponent UProceduralFoliageComponent::Get(AActor Actor, FName WithName = NAME_None)
```

GetOrCreate

### static UProceduralFoliageComponent UProceduralFoliageComponent::GetOrCreate

```
static UProceduralFoliageComponent UProceduralFoliageComponent::GetOrCreate(
```

AActor 	Actor,

### FName 	WithName	 = 	NAME_None

```
FName 	WithName	 = 	NAME_None
```

)

### Create

```
static UProceduralFoliageComponent UProceduralFoliageComponent::Create(AActor Actor, FName WithName = NAME_None)
```

StaticClass

### static UClass UProceduralFoliageComponent::StaticClass

```
static UClass UProceduralFoliageComponent::StaticClass()
```
