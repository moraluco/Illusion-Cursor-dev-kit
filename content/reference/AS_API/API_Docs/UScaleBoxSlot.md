# UScaleBoxSlot

**Visibility:** public

## Inheritance

UPanelSlot → UScaleBoxSlot

## Description

The Slot for the UScaleBoxSlot, contains the widget displayed in a button's single slot

LAYOUT|SCALEBOX SLOT HorizontalAlignment EHorizontalAlignment HorizontalAlignment

The alignment of the object horizontally.

VerticalAlignment EVerticalAlignment VerticalAlignment

The alignment of the object vertically.

Padding FMargin Padding

The padding area between the slot and the content it contains.

LAYOUT|SCALEBOX SLOT SetPadding void SetPadding( FMargin 	InPadding 	) SetVerticalAlignment void SetVerticalAlignment( EVerticalAlignment 	InVerticalAlignment 	) SetHorizontalAlignment void SetHorizontalAlignment( EHorizontalAlignment 	InHorizontalAlignment 	)

## FUNCTIONS

### GetHorizontalAlignment

```
const EHorizontalAlignment& GetHorizontalAlignment()const
```

GetVerticalAlignment

### const EVerticalAlignment& GetVerticalAlignment

```
const EVerticalAlignment& GetVerticalAlignment()const GetPadding
```

const FMargin& GetPadding() const

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UScaleBoxSlot::StaticClass()
```
