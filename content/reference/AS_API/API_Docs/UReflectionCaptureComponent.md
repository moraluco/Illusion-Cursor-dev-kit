# UReflectionCaptureComponent

**Visibility:** public

## Inheritance

USceneComponent → UReflectionCaptureComponent → UBoxReflectionCaptureComponent → UPlaneReflectionCaptureComponent → USphereReflectionCaptureComponent

## Description

-> will be exported to EngineDecalClasses.h

## REFLECTIONCAPTURE

### CaptureOffset

```
FVector CaptureOffset
```

World space offset to apply before capturing.

### Cubemap

```
UTextureCube Cubemap
```

Cubemap to use for reflection if ReflectionSourceType is set to RS_SpecifiedCubemap.

### SourceCubemapAngle

```
float32 SourceCubemapAngle
```

Angle to rotate the source cubemap when SourceType is set to SLS_SpecifiedCubemap.

### Brightness

```
float32 Brightness
```

A brightness control to scale the captured scene's reflection intensity.

### ReflectionSourceType

```
EReflectionSourceType ReflectionSourceType
```

Indicates where to get the reflection source from.

---

## REFLECTIONCAPTUREMOBILE

### MaxValueRGBM

```
float32 MaxValueRGBM
```

M values for RGBM, it will dynamically be generated by taking the highest RGB value into account. Explictly modifying it will require a Reflection Re-Capture.

### bModifyMaxValueRGBM

```
bool bModifyMaxValueRGBM
```

Indicates if we want to manually modify the MaxRGBM value.

### MobileReflectionCompression

```
EMobileReflectionCompression MobileReflectionCompression
```

Indicates if the Reflection will be compressed for mobile. If set to default it will take the project wide setting.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Get static UReflectionCaptureComponent UReflectionCaptureComponent::Get(AActor Actor, FName WithName = NAME_None)
```

GetOrCreate

### static UReflectionCaptureComponent UReflectionCaptureComponent::GetOrCreate

```
static UReflectionCaptureComponent UReflectionCaptureComponent::GetOrCreate(
```

AActor 	Actor,

### FName 	WithName	 = 	NAME_None

```
FName 	WithName	 = 	NAME_None
```

)

### Create

```
static UReflectionCaptureComponent UReflectionCaptureComponent::Create(AActor Actor, FName WithName = NAME_None)
```

StaticClass

### static UClass UReflectionCaptureComponent::StaticClass

```
static UClass UReflectionCaptureComponent::StaticClass()
```
