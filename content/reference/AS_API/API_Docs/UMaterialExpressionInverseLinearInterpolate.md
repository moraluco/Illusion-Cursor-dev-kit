# UMaterialExpressionInverseLinearInterpolate

**Visibility:** public

## Inheritance

UMaterialExpression → UMaterialExpressionInverseLinearInterpolate → MATERIALEXPRESSIONINVERSELINEARINTERPOLATE → ConstB

## Description

float32 ConstB

only used if B is not hooked up

ConstValue float32 ConstValue

only used if Value is not hooked up

bClampResult bool bClampResult

Clamp the result to 0 to 1

ConstA float32 ConstA

only used if A is not hooked up

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UMaterialExpressionInverseLinearInterpolate::StaticClass()
```
