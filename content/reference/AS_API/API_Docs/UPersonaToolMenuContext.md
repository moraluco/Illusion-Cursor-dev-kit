# UPersonaToolMenuContext

**Visibility:** public

## Inheritance

UObject → UPersonaToolMenuContext → PERSONAEDITOREXTENSIONS → AnimBlueprint

## Description

const UAnimBlueprint AnimBlueprint

Get the anim blueprint that we are editing

Mesh const USkeletalMesh Mesh

Get the skeletal mesh that we are editing

PreviewMeshComponent const UDebugSkelMeshComponent PreviewMeshComponent

Get the preview component that we are using

Skeleton const USkeleton Skeleton

Get the skeleton that we are editing

AnimationAsset const UAnimationAsset AnimationAsset

Get the animation asset that we are editing

## PERSONAEDITOREXTENSIONS

### GetAnimBlueprint

```
UAnimBlueprint GetAnimBlueprint()const
```

Get the anim blueprint that we are editing

### GetMesh

```
USkeletalMesh GetMesh()const
```

Get the skeletal mesh that we are editing

### GetPreviewMeshComponent

```
UDebugSkelMeshComponent GetPreviewMeshComponent()const
```

Get the preview component that we are using

### GetSkeleton

```
USkeleton GetSkeleton()const
```

Get the skeleton that we are editing

### GetAnimationAsset

```
UAnimationAsset GetAnimationAsset()const
```

Get the animation asset that we are editing

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UPersonaToolMenuContext::StaticClass()
```
