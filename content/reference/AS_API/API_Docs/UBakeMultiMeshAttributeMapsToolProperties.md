# UBakeMultiMeshAttributeMapsToolProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → UBakeMultiMeshAttributeMapsToolProperties → BAKEOUTPUT → MapPreview

## Description

FString MapPreview

The map type index to preview

MapTypes int MapTypes

The map types to generate

## TEXTURES

### BitDepth

```
EBakeTextureBitDepth BitDepth
```

The channel bit depth of the source data for the generated textures

### SamplesPerPixel

```
EBakeTextureSamplesPerPixel SamplesPerPixel
```

Number of samples per pixel

### Resolution

```
EBakeTextureResolution Resolution
```

The pixel resolution of the generated map

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UBakeMultiMeshAttributeMapsToolProperties::StaticClass()
```
