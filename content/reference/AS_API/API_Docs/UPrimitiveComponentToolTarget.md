# UPrimitiveComponentToolTarget

**Visibility:** public

## Inheritance

UToolTarget → UPrimitiveComponentToolTarget → UDynamicMeshComponentToolTarget → USkeletalMeshComponentReadOnlyToolTarget → UStaticMeshComponentToolTarget → UVolumeComponentToolTarget

## Description

A tool target to share some reusable code for tool targets that are backed by primitive components.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UPrimitiveComponentToolTarget::StaticClass()
```
