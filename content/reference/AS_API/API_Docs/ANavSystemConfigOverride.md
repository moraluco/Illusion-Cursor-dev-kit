# ANavSystemConfigOverride

**Visibility:** public

## Inheritance

AActor → ANavSystemConfigOverride → NAVIGATION → OverridePolicy

## Description

ENavSystemOverridePolicy OverridePolicy

If there's already a NavigationSystem instance in the world how should this nav override behave

bLoadOnClient bool bLoadOnClient NavigationSystemConfig UNavigationSystemConfig NavigationSystemConfig

## FUNCTIONS

### SetbLoadOnClient

```
void SetbLoadOnClient(bool Value)
```

GetbLoadOnClient

### bool GetbLoadOnClient

```
bool GetbLoadOnClient()const STATIC FUNCTIONS
```

Spawn

### static ANavSystemConfigOverride ANavSystemConfigOverride::Spawn

```
static ANavSystemConfigOverride ANavSystemConfigOverride::Spawn(
```

FVector 	Location	 = 	FVector :: ZeroVector,

### FRotator 	Rotation	 = 	FRotator :: ZeroRotator,

```
FRotator 	Rotation	 = 	FRotator :: ZeroRotator, FName 	Name	 = 	NAME_None, ULevel 	Level	 = 	nullptr
```

)

### StaticClass

```
static UClass ANavSystemConfigOverride::StaticClass()
```
