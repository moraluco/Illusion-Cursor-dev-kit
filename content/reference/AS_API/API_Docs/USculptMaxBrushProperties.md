# USculptMaxBrushProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → USculptMaxBrushProperties → SCULPTMAXBRUSH

## Description

bFreezeCurrentHeight bool bFreezeCurrentHeight

Use maximum height from last brush stroke, regardless of brush size. Note that spatial brush falloff still applies.

MaxHeight float32 MaxHeight

Specify maximum displacement height (relative to brush size)

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass USculptMaxBrushProperties::StaticClass()
```
