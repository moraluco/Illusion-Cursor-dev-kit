# ATextRenderActor

**Visibility:** public

## Inheritance

AActor → ATextRenderActor → TEXTRENDERACTOR → TextRender

## Description

UTextRenderComponent TextRender

Component to render a text in 3d with a font

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static ATextRenderActor ATextRenderActor::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass ATextRenderActor::StaticClass

```
static UClass ATextRenderActor::StaticClass()
```
