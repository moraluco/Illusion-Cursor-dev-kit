# ATestBeaconClient

**Visibility:** public

## Inheritance

AOnlineBeaconClient → ATestBeaconClient

## Description

A beacon client used for making reservations with an existing game session

## FUNCTIONS

### ServerPong

```
void ServerPong()
```

Send a pong RPC to the host

### ClientPing

```
void ClientPing()
```

Send a ping RPC to the client

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static ATestBeaconClient ATestBeaconClient::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass ATestBeaconClient::StaticClass

```
static UClass ATestBeaconClient::StaticClass()
```
