# UMovieSceneImageCaptureProtocolBase

**Visibility:** public

## Inheritance

UMovieSceneCaptureProtocolBase → UMovieSceneImageCaptureProtocolBase → UCompositionGraphCaptureProtocol → UFrameGrabberProtocol → UUserDefinedCaptureProtocol

## Description

A class to inherit from for image capture protocols. Used to filter the UI for protocols used on the image capture pass.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UMovieSceneImageCaptureProtocolBase::StaticClass()
```
