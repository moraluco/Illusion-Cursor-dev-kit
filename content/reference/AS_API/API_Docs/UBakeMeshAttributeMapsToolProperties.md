# UBakeMeshAttributeMapsToolProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → UBakeMeshAttributeMapsToolProperties → BAKEOUTPUT → MapPreview

## Description

FString MapPreview

The baked output type used for preview in the viewport

MapTypes int MapTypes

The bake output types to generate

## TEXTURES

### BitDepth

```
EBakeTextureBitDepth BitDepth
```

The bit depth for each channel of the generated textures

### SamplesPerPixel

```
EBakeTextureSamplesPerPixel SamplesPerPixel
```

Number of samples per pixel

### Resolution

```
EBakeTextureResolution Resolution
```

The pixel resolution of the generated textures

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UBakeMeshAttributeMapsToolProperties::StaticClass()
```
