# UGenlockedTimecodeProvider

**Visibility:** public

## Inheritance

UTimecodeProvider → UGenlockedTimecodeProvider

## Description

This timecode provider base class will try to use the engine genlock sync to adjust its count.

## SETTINGS

### bUseGenlockToCount

```
bool bUseGenlockToCount
```

Use Genlock Sync to update Timecode count

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UGenlockedTimecodeProvider::StaticClass()
```
