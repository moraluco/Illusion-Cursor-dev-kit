# AReflectionCapture

**Visibility:** public

## Inheritance

AActor → AReflectionCapture → ABoxReflectionCapture → APlaneReflectionCapture → ASphereReflectionCapture → DECALACTOR → CaptureComponent

## Description

UReflectionCaptureComponent CaptureComponent

Reflection capture component.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static AReflectionCapture AReflectionCapture::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass AReflectionCapture::StaticClass

```
static UClass AReflectionCapture::StaticClass()
```
