# ABlockingVolume

**Visibility:** public

## Inheritance

AVolume → ABlockingVolume

## Description

An invisible volume used to block other actors.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static ABlockingVolume ABlockingVolume::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass ABlockingVolume::StaticClass

```
static UClass ABlockingVolume::StaticClass()
```
