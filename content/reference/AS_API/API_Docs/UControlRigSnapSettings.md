# UControlRigSnapSettings

**Visibility:** public

## Inheritance

UObject → UControlRigSnapSettings

## Description

SNAP SETTINGS bSnapPosition bool bSnapPosition

When snapping, also snap position

bSnapRotation bool bSnapRotation

When snapping, also snap rotation

bSnapScale bool bSnapScale

When snapping, also snap scale

bKeepOffset bool bKeepOffset

When snapping keep offset, if false snap completely

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UControlRigSnapSettings::StaticClass()
```
