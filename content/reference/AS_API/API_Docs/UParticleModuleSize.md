# UParticleModuleSize

**Visibility:** public

## Inheritance

UParticleModuleSizeBase → UParticleModuleSize → UParticleModuleSize_Seeded → SIZE → StartSize

## Description

FRawDistributionVector StartSize

The initial size that should be used for a particle.  The value is retrieved using the EmitterTime during the spawn of a particle.  It is added to the Size and BaseSize fields of the spawning particle.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UParticleModuleSize::StaticClass()
```
