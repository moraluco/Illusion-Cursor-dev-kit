# ALevelScriptActor

**Visibility:** public

## Inheritance

AActor → ALevelScriptActor → AFunctionalTestLevelScript

## Description

ALevelScriptActor is the base class for classes generated by ULevelScriptBlueprints. ALevelScriptActor instances are hidden actors that exist within a level, and can execute level-wide logic (operating on specific actor instances within the level). The level-script's functionality is defined inside the ULevelScriptBlueprint itself (using the blueprint's node-based interface).

### See also

- AActor @see https://​docs​.unrealengine​.com​/latest​/INT​/Engine​/Blueprints​/UserGuide​/Types​/LevelBlueprint​/index​.html @see ULevelScriptBlueprint @see https://​docs​.unrealengine​.com​/latest​/INT​/Engine​/Blueprints​/index​.html @see UBlueprint

## GAME \| CINEMATIC

### SetCinematicMode

```
void SetCinematicMode(bool bCinematicMode, bool bHidePlayer = true, bool bAffectsHUD = true, bool bAffectsMovement = false, bool bAffectsTurning = false)
```

Sets the cinematic mode on all PlayerControllers

**Parameters**

bHidePlayer
bool

specify true to hide the player's pawn (only relevant if bInCinematicMode is true)

bAffectsHUD
bool

specify true if we should show/hide the HUD to match the value of bCinematicMode

bAffectsMovement
bool

specify true to disable movement in cinematic mode, enable it when leaving

bAffectsTurning
bool

specify true to disable turning in cinematic mode or enable it when leaving

---

## MISCELLANEOUS

### RemoteEvent

```
bool RemoteEvent(FName EventName)
```

Tries to find an event named "EventName" on all other levels, and calls it

---

## FUNCTIONS

### WorldOriginLocationChanged

```
void WorldOriginLocationChanged(FIntVector OldOriginLocation, FIntVector NewOriginLocation)
```

Event called on world origin location changes

**Parameters**

OldOriginLocation
FIntVector

Previous world origin location

NewOriginLocation
FIntVector

New world origin location

LevelReset
void LevelReset()

@todo document

STATIC FUNCTIONS
Spawn
static ALevelScriptActor ALevelScriptActor::Spawn(
FVector 	Location	 = 	FVector :: ZeroVector,
FRotator 	Rotation	 = 	FRotator :: ZeroRotator,
FName 	Name	 = 	NAME_None,
ULevel 	Level	 = 	nullptr
	)
StaticClass
static UClass ALevelScriptActor::StaticClass()
