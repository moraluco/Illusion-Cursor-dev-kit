# UMaterialExpressionDynamicParameter

**Visibility:** public

## Inheritance

UMaterialExpression → UMaterialExpressionDynamicParameter → MATERIALEXPRESSIONDYNAMICPARAMETER → DefaultValue

## Description

FLinearColor DefaultValue ParameterIndex uint ParameterIndex

The index of the dynamic parameter for use in tools that allow > 1

ParamNames TArray<FString> ParamNames

The names of the parameters.  These will show up in Cascade when editing a particle system that uses the material it is in...

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UMaterialExpressionDynamicParameter::StaticClass()
```
