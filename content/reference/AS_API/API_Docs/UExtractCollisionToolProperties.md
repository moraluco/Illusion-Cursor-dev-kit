# UExtractCollisionToolProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → UExtractCollisionToolProperties → OPTIONS

## Description

bWeldEdges bool bWeldEdges

Whether or not to weld coincident border edges of the Complex Collision Mesh (if possible)

bOutputSeparateMeshes bool bOutputSeparateMeshes

Whether or not to generate a seperate Mesh Object for each Simple Collision Shape

bShowPreview bool bShowPreview

Show/Hide a preview of the generated mesh (overlaps source mesh)

bShowInputMesh bool bShowInputMesh

Show/Hide input mesh

CollisionType EExtractCollisionOutputType CollisionType

Type of collision geometry to convert to Mesh

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UExtractCollisionToolProperties::StaticClass()
```
