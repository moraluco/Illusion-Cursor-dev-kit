# UEdMode

**Visibility:** public

## Inheritance

UObject → UEdMode → UBaseLegacyWidgetEdMode → ULegacyEdModeWrapper → UMeshPaintMode

## Description

Base class for all editor modes.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UEdMode::StaticClass()
```
