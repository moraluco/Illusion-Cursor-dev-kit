# SequenceRecorder

## Inheritance

SequenceRecorder

## Description

SEQUENCE RECORDING StartRecordingSequence static void SequenceRecorder::StartRecordingSequence( TArray<AActor> 	ActorsToRecord 	)

Start recording the passed-in actors to a level sequence.

Parameters ActorsToRecord TArray<AActor>

The actors to record

StopRecordingSequence static void SequenceRecorder::StopRecordingSequence()

Stop recording the current sequence, if any

IsRecordingSequence static bool SequenceRecorder::IsRecordingSequence()

Are we currently recording a sequence
