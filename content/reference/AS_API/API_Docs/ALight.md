# ALight

**Visibility:** public

## Inheritance

AActor → ALight → ADirectionalLight → APointLight → ARectLight → ASpotLight → LIGHT → LightComponent

## Description

ULightComponent LightComponent

@todo document

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static ALight ALight::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass ALight::StaticClass

```
static UClass ALight::StaticClass()
```
