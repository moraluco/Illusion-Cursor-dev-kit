# UWeldMeshEdgesToolProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → UWeldMeshEdgesToolProperties → OPTIONS

## Description

bOnlyUnique bool bOnlyUnique

Only merge unambiguous pairs that have unique duplicate-edge matches

Tolerance float32 Tolerance

Edges are considered matching if both pairs of endpoint vertices are closer than this distance

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UWeldMeshEdgesToolProperties::StaticClass()
```
