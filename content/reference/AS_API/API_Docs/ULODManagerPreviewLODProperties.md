# ULODManagerPreviewLODProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → ULODManagerPreviewLODProperties → LODPREVIEW

## Description

bShowSeams bool bShowSeams

Control whether mesh borders are displayed

VisibleLOD FString VisibleLOD

LOD to visualise. Default option is equivalent to disabling the Tool, RenderData is the mesh used for rendering derived from the SourceModel (possibly simplified)

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass ULODManagerPreviewLODProperties::StaticClass()
```
