# AAROriginActor

**Visibility:** public

## Inheritance

AActor → AAROriginActor

## Description

Simple actor that is spawned at the origin for AR systems that want to hang components on an actor Spawned as a custom class for easier TObjectIterator use

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Spawn static AAROriginActor AAROriginActor::Spawn(FVector Location = FVector :: ZeroVector, FRotator Rotation = FRotator :: ZeroRotator, FName Name = NAME_None, ULevel Level = nullptr)
```

StaticClass

### static UClass AAROriginActor::StaticClass

```
static UClass AAROriginActor::StaticClass()
```
