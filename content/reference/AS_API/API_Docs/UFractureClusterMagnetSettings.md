# UFractureClusterMagnetSettings

**Visibility:** public

## Inheritance

UFractureToolSettings → UFractureClusterMagnetSettings → CLUSTERMAGNET → Iterations

## Description

uint Iterations

How widely to search for neighboring bones to add to the new clusters: When Iterations is 1, only direct neighbors of the selected bones can be added to the new clusters; increasing Iterations allows additional layers of neighbors-of-neighbors to be added

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UFractureClusterMagnetSettings::StaticClass()
```
