# USlateThemeManager

**Visibility:** public

## Inheritance

UObject → USlateThemeManager → COLORS → ActiveColors

## Description

FStyleColorList ActiveColors CurrentThemeId FGuid CurrentThemeId STATIC FUNCTIONS StaticClass static UClass USlateThemeManager::StaticClass()
