# USubsystemLibrary

**Visibility:** public

## Inheritance

UObject → USubsystemLibrary

## Description

These functions are blueprint internal by default, but we need them exposed in Angelscript

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass USubsystemLibrary::StaticClass()
```
