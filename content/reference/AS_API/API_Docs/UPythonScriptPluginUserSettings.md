# UPythonScriptPluginUserSettings

**Visibility:** public

## Inheritance

UDeveloperSettings → UPythonScriptPluginUserSettings → PYTHON

## Description

bEnableContentBrowserIntegration bool bEnableContentBrowserIntegration

Should Python scripts be available in the Content Browser?

bDeveloperMode bool bDeveloperMode

Should Developer Mode be enabled on the Python interpreter?

(will also enable extra warnings (e.g., for deprecated code), and enable stub code generation for use with external IDEs).

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UPythonScriptPluginUserSettings::StaticClass()
```
