# USoundEffectSourcePreset

**Visibility:** public

## Inheritance

USoundEffectPreset → USoundEffectSourcePreset → USourceEffectBitCrusherPreset → USourceEffectChorusPreset → USourceEffectDynamicsProcessorPreset → USourceEffectEnvelopeFollowerPreset

## Description

and 11 other children

Preset of a source effect that can be shared between chains.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass USoundEffectSourcePreset::StaticClass()
```
