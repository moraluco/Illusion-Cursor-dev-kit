# UOnAcceptHandleSourcesPropertiesBase

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → UOnAcceptHandleSourcesPropertiesBase → UOnAcceptHandleSourcesProperties → UOnAcceptHandleSourcesPropertiesSingle

## Description

Base class for property settings for tools that create a new actor and need to decide what to do with the input objects.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UOnAcceptHandleSourcesPropertiesBase::StaticClass()
```
