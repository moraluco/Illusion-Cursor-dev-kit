# UGizmoHandleGroup

**Visibility:** public

## Inheritance

USceneComponent → UGizmoHandleGroup → UAxisGizmoHandleGroup → UStretchGizmoHandleGroup → UUniformScaleGizmoHandleGroup

## Description

Base class for gizmo handles

### STATIC FUNCTIONS

```
STATIC FUNCTIONS Get static UGizmoHandleGroup UGizmoHandleGroup::Get(AActor Actor, FName WithName = NAME_None)
```

GetOrCreate

### static UGizmoHandleGroup UGizmoHandleGroup::GetOrCreate

```
static UGizmoHandleGroup UGizmoHandleGroup::GetOrCreate(AActor Actor, FName WithName = NAME_None)
```

Create

### static UGizmoHandleGroup UGizmoHandleGroup::Create

```
static UGizmoHandleGroup UGizmoHandleGroup::Create(AActor Actor, FName WithName = NAME_None)
```

StaticClass

### static UClass UGizmoHandleGroup::StaticClass

```
static UClass UGizmoHandleGroup::StaticClass()
```
