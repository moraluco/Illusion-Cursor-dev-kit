# UBakeVisualizationProperties

**Visibility:** public

## Inheritance

UInteractiveToolPropertySet → UBakeVisualizationProperties → PREVIEW → Brightness

## Description

float32 Brightness

Adjust the brightness of the preview material; does not affect results stored in textures

AOMultiplier float32 AOMultiplier

Ambient Occlusion multiplier in the viewport; does not affect results stored in textures

bPreviewAsMaterial bool bPreviewAsMaterial

Preview the texture as an input to the respective material parameter (ex. Normal, Ambient Occlusion)

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UBakeVisualizationProperties::StaticClass()
```
