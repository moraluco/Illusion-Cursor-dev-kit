# ENiagaraTickBehavior

## Inheritance

ENiagaraTickBehavior → TYPES → ENiagaraTickBehavior → UsePrereqs

## Description

Enum

UseComponentTickGroup

Enum

ForceTickFirst

Enum

ForceTickLast

Enum

ENiagaraTickBehavior_MAX

Enum
