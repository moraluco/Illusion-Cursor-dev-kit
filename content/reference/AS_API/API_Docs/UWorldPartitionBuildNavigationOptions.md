# UWorldPartitionBuildNavigationOptions

**Visibility:** public

## Inheritance

UObject → UWorldPartitionBuildNavigationOptions

## Description

BUILD NAVIGATION bCleanPackages bool bCleanPackages

When enabled, delete all NavigationDataChunkActors instead of generating them.

bVerbose bool bVerbose

Use verbose logging.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UWorldPartitionBuildNavigationOptions::StaticClass()
```
