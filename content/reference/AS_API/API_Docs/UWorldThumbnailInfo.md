# UWorldThumbnailInfo

**Visibility:** public

## Inheritance

USceneThumbnailInfo → UWorldThumbnailInfo → THUMBNAIL → OrthoDirection

## Description

EOrthoThumbnailDirection OrthoDirection

The direction of the camera when in Orthographic CameraMode

CameraMode ECameraProjectionMode CameraMode

The type of projection to use

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UWorldThumbnailInfo::StaticClass()
```
