# UTextureEncodingUserSettings

**Visibility:** public

## Inheritance

UDeveloperSettings → UTextureEncodingUserSettings → ENCODESPEEDS → ForceEncodeSpeed

## Description

ETextureEncodeSpeedOverride ForceEncodeSpeed

Local machine/project setting to force an encode speed, if desired.  See the Engine "Texture Encoding" section for details.

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass UTextureEncodingUserSettings::StaticClass()
```
