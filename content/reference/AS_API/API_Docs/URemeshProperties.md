# URemeshProperties

**Visibility:** public

## Inheritance

UMeshConstraintProperties → URemeshProperties → UBrushRemeshProperties → URemeshMeshToolProperties → REMESHING → SmoothingStrength

## Description

float32 SmoothingStrength

Amount of Vertex Smoothing applied within Remeshing

REMESHING|EDGE CONSTRAINTS bSplits bool bSplits

Enable edge splits in Remeshing

bCollapses bool bCollapses

Enable edge collapses in Remeshing

bFlips bool bFlips

Enable edge flips in Remeshing (tends to lose edges/detail)

### STATIC FUNCTIONS

```
STATIC FUNCTIONS StaticClass static UClass URemeshProperties::StaticClass()
```
